% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom-text-3d.R
\name{rotate_text_to_facing}
\alias{rotate_text_to_facing}
\title{Rotate and position text vertices in 3D space}
\usage{
rotate_text_to_facing(
  vertices,
  facing_normal,
  angle,
  anchor_x,
  anchor_y,
  anchor_z,
  size_mm,
  data_span,
  aspect_adjust = 1
)
}
\arguments{
\item{vertices}{Data frame with vertex_x, vertex_y columns (text outline vertices in points)}

\item{facing_normal}{Unit vector for the direction the text should face}

\item{angle}{Rotation angle around the facing axis (degrees)}

\item{anchor_x, anchor_y, anchor_z}{Position to place the text}

\item{size_mm}{Text size in mm (same units as geom_text)}

\item{data_span}{The span of the data in data units (used to compute conversion)}

\item{aspect_adjust}{Scalar multiplier for text y-coordinates (height).
Values > 1 make text taller, values < 1 make text wider.}
}
\value{
Data frame with x, y, z columns in data coordinates
}
\description{
Takes 2D text vertices (in x-y plane) and transforms them to face the
specified direction at the specified position. With angle=0, the text
baseline will be as horizontal as possible (parallel to x-y plane when
the facing direction allows).
}
\keyword{internal}
