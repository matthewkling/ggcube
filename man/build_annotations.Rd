% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annotate-3d.R
\name{build_annotations}
\alias{build_annotations}
\title{Build annotation rows from annotation specs}
\usage{
build_annotations(annotate, max_group = 0)
}
\arguments{
\item{annotate}{A single \code{annotate_3d} object, or a list of them.}

\item{max_group}{Numeric. The maximum existing group number, used to
generate non-colliding group IDs for annotation rows.}
}
\value{
A data frame of annotation rows, or \code{NULL} if \code{annotate} is NULL.
}
\description{
Takes annotation specifications (single or list) and generates data-space
rows tagged with \code{.prim} and dot-prefixed style columns, ready to be
bound onto primary data in \code{setup_data} so that annotation positions
participate in scale training.
}
\details{
For annotation types that need panel range information (e.g. planes),
positional columns may contain sentinel values (NA) that are resolved
later in \code{draw_panel} via \code{resolve_annotations()}.
}
\keyword{internal}
