% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/depth-sort.R
\name{split_interpenetrating_segments}
\alias{split_interpenetrating_segments}
\title{Split segments at polygon interpenetration points}
\usage{
split_interpenetrating_segments(
  data,
  grp_data,
  grp_idx,
  prim_lookup,
  poly_betas
)
}
\arguments{
\item{data}{Data frame with columns x, y, depth (post-transform), group,
.prim, and all aesthetic columns.}

\item{grp_data}{List of per-group matrices from pw_render_order.}

\item{grp_idx}{List of row index vectors per group, keyed by group name.}

\item{prim_lookup}{Named character vector of primitive types per group.}

\item{poly_betas}{List of pre-computed plane beta vectors.}
}
\value{
Data frame with interpenetrating segments replaced by sub-segments.
Non-segment rows are returned unchanged.
}
\description{
For each segment, finds all polygons it passes through in 3D (i.e., the
segment is in front of the polygon at one end and behind at the other).
Computes the parameter t where segment depth equals polygon depth, and
splits the segment at all such crossing points.
}
\details{
Handles three overlap cases:
\itemize{
\item Both segment endpoints inside polygon footprint
\item One endpoint inside, one outside
\item Both endpoints outside but segment crosses through polygon
}
}
\keyword{internal}
