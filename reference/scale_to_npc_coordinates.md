# Scale transformed coordinates to final npc coordinates

Scale transformed coordinates to final npc coordinates

## Usage

``` r
scale_to_npc_coordinates(x, y = NULL, plot_bounds)
```

## Arguments

- plot_bounds:

  Plot bounds vector xmin, xmax, ymin, ymax

- result:

  Data frame with transformed x, y coordinates

## Value

Data frame with scaled coordinates
