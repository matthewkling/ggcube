# Dispatch to the correct renderer for a single primitive type

Dispatch to the correct renderer for a single primitive type

## Usage

``` r
render_single_type(data, prim_type, rule, arrow, lineend)
```
