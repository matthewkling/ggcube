# Compute normalized bar statistics

Compute normalized bar statistics

## Usage

``` r
compute_bar_stats(agg_data)
```

## Arguments

- agg_data:

  Data frame with count and bin_area columns

## Value

Data frame with added proportion, ncount, density, ndensity columns
