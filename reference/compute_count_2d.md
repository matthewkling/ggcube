# Count discrete x-y combinations

Count discrete x-y combinations

## Usage

``` r
compute_count_2d(data, drop = TRUE)
```

## Arguments

- data:

  Data frame with x, y, weight columns

- drop:

  Whether to drop empty combinations

## Value

Data frame with x, y, count, bin_area columns
