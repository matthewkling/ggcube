# Convert irregular points to triangular tiles via Delaunay triangulation

Convert irregular points to triangular tiles via Delaunay triangulation

## Usage

``` r
delaunay_points_to_tiles(data, group_prefix)
```
