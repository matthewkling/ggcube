# Store lighting specification to data frame for downstream use

Store lighting specification to data frame for downstream use

## Usage

``` r
attach_light(data, light)
```

## Arguments

- data:

  Data frame to attach lighting spec to

- light:

  Lighting specification object, "none", or NULL

## Value

Data frame with lighting_spec column added (if light is not NULL)
