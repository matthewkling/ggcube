# Create quads or triangles from grid data

Create quads or triangles from grid data

## Usage

``` r
convert_to_tiles(data, grid = c("tri1", "tri2", "quad"))
```

## Arguments

- data:

  Regular grid data frame

## Value

Data frame with quad faces and computed gradients
