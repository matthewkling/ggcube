# Render text primitives (billboard)

Each group is a single text label. Rendered as individual textGrobs
assembled into a grobTree.

## Usage

``` r
render_texts(data)
```

## Arguments

- data:

  Text data with x, y, label, and aesthetic columns.

## Value

A grobTree of textGrobs.
